DONE: sdl2 display [Use SDL2 to render the pixels from the PPU]
TODO: Test suites central service [A test suite dashboard and db which will display the test status with timestamps and commit hashes]
TODO: Implement full APU timing behavior [src/apu.c is minimal (length counters + NR52 on/off only), so 70 SameSuite APU tests fail with serial 424242424242. Add the full frame sequencer (256 Hz / 512 Hz / 128 Hz steps with CPU-cycle-accurate phase), frequency sweep (NR10/NR11), volume envelope (NRx2), waveform generation/period behavior, wave RAM access timing + locking (channel 3), noise LFSR (channel 4), DAC on/off glitches, and trigger/restart semantics. Failing groups: apu/channel_1 (21), apu/channel_2 (15), apu/channel_3 (14), apu/channel_4 (13), apu/div_* (5). Also unblocks the blargg sound ROMs: dmg_sound/cgb_sound 04-sweep, 05-sweep details, 06-overflow on trigger (channel off when sweep freq > 0x7FF), 07-len sweep period sync (frame sequencer phase), 09-wave read while on, 10-wave trigger while on (phase reset), 12-wave write while on, plus the dmg_sound/cgb_sound composite ROMs]
TODO: Model M-cycle-accurate instruction memory-access timing [instr() executes each opcode atomically (total cycles + immediate bus reads/writes), so blargg mem_timing fails on all 8 tests. Memory accesses must occur at the correct dot within each instruction's M-cycles. Failing: 01-read_timing (F0 LDH A,(a8), FA LD A,(a16), CB 46/4E/56/5E/66/6E/76/7E BIT n,(HL)), 02-write_timing (36 LD (HL),d8, E0 LDH (a8),A, EA LD (a16),A), 03-modify_timing (35/34 INC/DEC (HL), all CB 06-FE ops on (HL)), plus composite mem_timing and mem_timing-2]
TODO: Implement the DMG OAM scan/corruption bug [ppu_render_sprites reads OAM directly at render time; the hardware instead copies sprite data into an internal buffer during mode 2, so CPU writes to OAM (0xFE00-0xFE9F) during OAM scan corrupt the buffer. This also needs dot-accurate writes. Failing blargg oam_bug tests: composite, 2-causes (LD DE,$FE00 : INC DE), 4-scanline_timing, 5-timing_bug, 7-timing_effect, 8-instr_effect]
TODO: Implement OAM DMA and CGB GDMA/HDMA [bus.c has no DMA support. Add OAM DMA (write 0xFF46 -> copy 160 bytes from 0xXX00-0xXX9F to OAM) and CGB General/HBlank DMA (0xFF51-0xFF55, incl. the LCD-off transfer and GDMA address masking). Failing SameSuite tests: dma/gbc_dma_cont, dma/gdma_addr_mask, dma/hdma_lcd_off, dma/hdma_mode0]
TODO: Implement CGB palette registers (BGPI/BGPD) [ppu.c only handles DMG registers; 0xFF68/0xFF69 (BGPI/BGPD) read back 0xFF. Add CGB palette RAM with index auto-increment (and the blocking behavior when bits 6-7 are set). Failing SameSuite test: ppu/blocking_bgpi_increase]
TODO: Fix EI delay + HALT interaction [interrupt/ei_delay_halt fails with serial 424242424242. Verify IME scheduling in cpu.c (ime_scheduled applied at the next cpu_step) against the EI;HALT case: IME must be enabled while the CPU is halted so pending interrupts wake it, matching the SameSuite expectation]
TODO: Implement SGB MULTI request (MLT_REQ) [No SGB support exists, so sgb/command_mlt_req and sgb/command_mlt_req_1_incrementing fail. Add P15 write handling for SGB commands (at minimum the MLT_REQ MULTI controller layout command) or filter SGB tests from the samesuite run]
TODO: Audio interface [An audio module to generate sound from the instructions]
TODO: Debugger [Debug mode which will allow the user to break and proceed step by step on the instructions]
